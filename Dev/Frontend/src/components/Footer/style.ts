import styled from "styled-components";

export const FooterGeneral = styled.footer`
display:flex;
justify-content:center;
align-items:center;
height:10%;


svg{
    circle{
        stroke:var(--thirty);
    }
    path{
        fill:var(--thirty);
    }
}
`